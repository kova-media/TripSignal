// Customer quotes for the homepage. Add real quotes here — the section
// renders nothing until at least one quote exists. Never invent quotes.
export const TESTIMONIALS: { quote: string; name: string; detail: string }[] = [];
